<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textAppearance">@style/CustomTextAppearance</item>
    </style>

    <style name="ActivityLogin" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:textAppearance">@style/CustomTextAppearance</item>
    </style>

    <style name="CustomTextAppearance">
        <item name="android:typeface">sans</item>
    </style>

    <style name="TextLabel" parent="TextAppearance.AppCompat">
        <!-- Text hint -->
        <item name="android:textColorHint">@color/teal</item>
        <!--Bottom line-->
        <item name="colorAccent">@color/peachLight</item>
        <!--Underline-->
        <item name="colorControlNormal">@color/colorPrimary</item>
        <!--Underline & text activated-->
        <item name="colorControlActivated">@color/skyblue</item>
    </style>


</resources>
